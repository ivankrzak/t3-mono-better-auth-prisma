{
  "name": "@acme/db",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./src/index.ts"
    },
    "./client": {
      "types": "./dist/client.d.ts",
      "default": "./src/client.ts"
    },
    "./schema": {
      "types": "./dist/prisma/schema.d.ts",
      "default": "./src/prisma/schema.ts"
    }
  },
  "license": "MIT",
  "scripts": {
    "build": "prisma generate && tsc",
    "clean": "git clean -xdf .cache .turbo dist node_modules",
    "dev": "tsc",
    "format": "prettier --check . --ignore-path ../../.gitignore",
    "lint": "eslint --flag unstable_native_nodejs_ts_config",
    "typecheck": "tsc --noEmit --emitDeclarationOnly false",
    "with-env": "dotenv -e ../../.env --",
    "push": "pnpm with-env prisma db push && pnpm db:generate",
    "db:generate": "pnpm with-env prisma generate",
    "db:migrate": "pnpm with-env prisma migrate dev && pnpm db:generate",
    "db:deploy": "pnpm with-env prisma migrate deploy",
    "db:reset": "pnpm with-env prisma migrate reset && pnpm db:seed && pnpm db:generate",
    "db:studio": "pnpm with-env prisma studio --port 5556",
    "db:seed": "pnpm with-env tsx src/seed.ts"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.8.0",
    "@prisma/client": "^7.8.0",
    "@vercel/postgres": "^0.10.0",
    "pg": "^8.16.3",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@acme/eslint-config": "workspace:*",
    "@acme/prettier-config": "workspace:*",
    "@acme/tsconfig": "workspace:*",
    "@types/node": "^22.14.1",
    "eslint": "catalog:",
    "prettier": "catalog:",
    "prisma": "^7.8.0",
    "typescript": "catalog:"
  },
  "prettier": "@acme/prettier-config"
}
